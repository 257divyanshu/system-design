It means **Internet routers are configured to ignore/drop traffic** destined for private IP ranges — they only forward traffic for public IPs.

So packets with private IPs never leave local networks.
