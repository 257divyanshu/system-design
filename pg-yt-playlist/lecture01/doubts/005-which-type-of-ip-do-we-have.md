Most normal users have a **dynamic IP** — your ISP assigns it via DHCP and it can change periodically or when your router restarts.

**Static IPs** are usually given only on request or to businesses/servers.
